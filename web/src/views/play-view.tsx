// Play: amp model/cab, control knobs, pedalboard, presets. Real content
// lands in P4b-5; the dev probe lives here until then.

import { ConnectionProbe } from "@/components/dev/connection-probe"

export function PlayView() {
  return <ConnectionProbe />
}
