build/
*.user
.vs/
__pycache__/
# Top-level installer output only; web/dist/index.html (the committed embedded
# UI build) is governed by web/.gitignore.
/dist/
captures-*.wav

# Local-dev tones only — copyrighted, never commit (see helper/starter/ for the shippable pack)
assets/

# Hosted /app is a build output (pnpm --dir web build:app). The landing page's
# hand-written files under site/ ARE committed; the generated app is not.

# Cloudflare Wrangler local state
.wrangler/
