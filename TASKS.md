# RiffAmp — task list

Guitar amp as a web app: browser page = the whole UI; tiny native helper = the
real-time audio engine (ASIO on Windows, CoreAudio on macOS). Validated by P0
on 2026-07-18: native WASAPI-exclusive round-trip **14.4 ms** @64 buffer vs
**79.6 ms** in-browser
(full research: [`../amp-latency-research/REPORT.md`](../amp-latency-research/REPORT.md)).

Product direction settled 2026-08-08: **demo-first funnel** — the hosted app
plays instantly in-browser via a WASM engine (~30 ms, good enough to fall in
love with the tone), and the 2 MB native helper is the opt-in upgrade to <10 ms.
Landing page = static SEO page; app = client-side React SPA (no SSR — app state
lives in the local helper, nothing to server-render).

Status legend: `[ ]` pending · `[~]` in progress · `[x]` done

## P1 — Native tone proof

- [x] **1. Install C++ toolchain** — VS Build Tools 2022 (17.14, MSVC 14.44) + CMake 4.4.0 via winget, 2026-07-18. *(Machine notes: neither is on the interactive PATH — use full paths `C:\Program Files\CMake\bin\cmake.exe`; interactive shell also resolves `python`/`py` incorrectly — use explicit paths / project venvs.)*
- [x] **2. P1a: C++ ASIO passthrough + RTL measurement** — DONE 2026-07-18. `helper/` builds (CMake + PortAudio master + ASIO SDK via FetchContent, VS generator). **MEASURED on "Komplete Audio ASIO Driver" @48 kHz: 7.62 ms RTL @64 buffer, 10.96 ms @128 — zero jitter across impulses. Target <12 ms BEATEN.** C++ WASAPI-exclusive = 13.6 ms (so Python overhead in P0 was only ~1 ms; ASIO driver removes the rest). Scoreboard: browser 79.6 → ASIO 7.62 ms.
- [x] **3. P1b: Amp tone engine (NAM + cab IR)** — DONE 2026-07-18. Built directly on NeuralAmpModelerCore v0.5.4 + FFTConvolver + dr_wav (`helper/src/amp.cpp`, `p1b bench|run`). SlimmableContainer models work (needed WHOLE_ARCHIVE — NAM's parsers self-register via static initializers). Bench @64: avg 8.2% of budget, 0/22500 blocks over, 12.2× realtime. **User played JCM800 Lead + Greenback IR live — confirmed working.** Deferred into P2: gate + tone-stack EQ stages (current chain: gain → NAM → IR → volume), runtime param/model/IR switching.

## P2 — Control plane

- [x] **4. P2a: WebSocket control server** — DONE 2026-07-18. `webamp-helper.exe --assets <dir>` (`helper/src/helper.cpp` + `dsp_extra.h`): full chain now gain → **gate** → NAM → **tone stack (bass/mid/treble)** → IR → volume, all params atomic + live; model/IR hot-swap via atomic pointer exchange while the stream runs; JSON protocol (hello/state/setParam/setModel/setIr/panic + meters ~15 Hz) over IXWebSocket on 127.0.0.1:43717; Origin allowlist verified (evil origin closed 1008, localhost served). Protocol regression test: `helper/tests/ws_test.py` — 15/15 PASS against the live engine. *Deferred: pairing codes + hosted-origin allowlist → P2c; model/IR upload via binary frames (currently assets-dir listing) → P2c; setDevice/setBufferSize (needs stream restart) → P3.*
- [x] **5. P2b: LNA permission checkpoint** — DONE 2026-07-18. Findings: (a) **Zen (user's daily browser, Firefox fork): no LNA prompt today** — `ws://127.0.0.1` from public HTTPS opens directly (Gecko loopback-WS gating is an open Mozilla bug; expect a prompt in a future release); (b) in-app Chromium 148 (Electron): LNA permissions queryable (`loopback-network` etc., "denied") but not enforced — WS opened anyway; (c) helper's Origin allowlist rejected the public origin with close 1008 in both — server-side defense carries the security regardless of browser behavior; (d) loopback mixed-content exemption confirmed (ws:// from https:// works). (e) **Consumer Chrome, observed by user Jul 18 2026: LNA permission chip appears** for `ws://127.0.0.1` from a public HTTPS origin; on deny/dismiss the handshake aborts browser-side (`ERR_CONNECTION_ABORTED`, close 1006 — never reaches the helper). Allow-path (chip → grant → open → helper origin-check) still worth one observation. P3's hosted-origin UX must explain the chip before connecting and show recovery guidance on denial (re-enable via the padlock/site-settings). Localhost page → helper → live engine state round trip verified (web/index.html).
- [x] **6. P2c: Minimal web UI** — DONE 2026-07-18. `web/index.html` (vanilla JS, no build step, served with any static server on localhost): rotary knobs (drag/wheel/double-click-reset) for gain/gate/bass/mid/treble/volume, amp-channel buttons, IR dropdown (72 cabs), live in/out meters, mute, auto-reconnect + "engine not found" card. Verified end-to-end in-browser: knob drag → engine param confirmed via independent client; model switch persists across page reload (server owns state). Fixed: param flush used requestAnimationFrame, which suspends in background/embedded tabs and silently dropped changes — now setTimeout(33 ms). Protocol suite re-run: 15/15. **MILESTONE HIT: knob on a web page changes the amp at ~7.6 ms.** *Deferred to P3: pairing/hosted-origin flow, model/IR upload from page, presets, metronome, mobile layout.*

## P3 — Productize *(split into real tasks when P2 lands; blocked by 3, 6)*

- [x] **P3a: Tray app** — DONE 2026-07-18. `webamp-helper.exe` is now a self-contained tray app: WIN32 subsystem (no console; logs to `webamp-helper.log` beside the exe), **serves its own UI** at `http://127.0.0.1:43718` (index.html embedded at build time via `cmake/embed_file.cmake`; WS stays on 43717), tray menu Open webamp / Toggle mute / Quit (graceful shutdown), assets default to `<exe>\assets` (`--assets` overrides), MessageBox on fatal errors. Verified: served page byte-identical to source, 404 on other paths, ws_test 15/15 against the tray build, file:// origin rejected. *Manual check pending: tray icon/menu UX (user).* Remaining tray polish → installer task: custom icon (currently stock), autostart option.
- [x] **P3b: Installer** — DONE 2026-07-18. `installer/webamp.iss` → `dist/webamp-setup-0.2.0.exe` (4.8 MB; build: Release build then `ISCC.exe installer\webamp.iss`, Inno Setup 6 via winget). Per-user install to `%LOCALAPPDATA%\Programs\webamp` (no admin, PrivilegesRequired=lowest), Start-menu shortcut, optional desktop icon + HKCU-Run autostart (both unchecked by default), kills running engine before install/uninstall, starter assets bundled (user's TONE3000 pack — swap for a curated/licensed pack before public distribution). Exe has embedded amber-note icon (`resources/webamp.ico` generated by Pillow, `.rc` resource). Verified: silent install, Programs & Features entry, installed copy runs + ws_test 15/15. **Signing decision: unsigned for now** (personal use; SmartScreen will warn other downloaders) — revisit with Azure Trusted Signing (~$10/mo) or SignPath (free OSS) when distributing.
- [ ] **Latency measurement wizard** — guided physical-loopback RTL test in the app (reuse the P1a impulse method as a protocol command + UI flow: "cable PHONES→IN 2, click measure"). Rationale: driver-reported latency (now shown in the UI, added 2026-07-18) is an estimate — ASIO under-reports by ~2 ms (5.3 reported vs 7.62 measured here), WASAPI over-reports wildly (43 vs 13.6). Never display an unmeasured number as a promise.
- [x] **P3c: Engine metronome + per-track volumes** — DONE 2026-07-18. Click synth runs **in the audio callback** (sample-accurate, same 7.6 ms path as the guitar; avoids ASIO-vs-browser device contention): accented decaying-sine clicks (1600/1100 Hz, ~25 ms tail), mixed post-chain as `guitar×VOLUME + click×CLICK`. Params: metroOn/metroBpm(30–300)/metroBeats(1–8)/metroVol(0–2), all live via setParam. UI: metronome section (start/stop, tempo slider, beats, CLICK knob). Verified: `helper/tests/metro_test.py` — params round-trip, clamping, and clicks audible on the OUT meter then silent after stop; ws_test 15/15. *Superseded same day by P3d below.*
- [x] **P3d: Metronome UX ported from the standalone app** — DONE 2026-07-18. The user's metronome ([`../metronome/`](../metronome/)) UX now lives in webamp's metronome section, restyled to the amber/dark palette: **visual beat dots driven by engine beat events** (`beatCount`/`beatInBar` in meters messages, now ~25 Hz), tap tempo (4-interval average), editable BPM with out-of-range red flash, ±step buttons (step in localStorage), beats/bar 1–12, accent-first toggle (new engine param `metroAccent`), keyboard Space/↑/↓/T. Engine BPM range widened to 20–360. Verified: browser-driven start/nudge → engine confirmed independently; both test suites pass. Standalone app remains untouched.
- [x] **P3e: Tuner** — DONE 2026-07-18. YIN pitch detection (`helper/src/tuner.h`, de Cheveigné/Kawahara) over a lock-free ring of pre-gate input samples, run ~12 Hz on the control thread (never the audio callback). Offline algorithm test `tuner_test` (CMake target): **sub-0.1-cent accuracy** on all standard strings, harmonic-rich plucks, ±50-cent detunes, drop C/D; silence → no pitch. `{type:"tuner"}` messages carry freq/note/cents; UI section (top of page): note display, ±50-cent needle, green within ±5 cents, freq readout. Protocol checks added to ws_test (18/18).
- [x] **P3g: Audio device + channel selection / UI regroup** — DONE 2026-07-18. Engine: `enumerateDevices` (ASIO/WASAPI/WDM-KS), `AudioIO` stream-restart manager, **config persistence** (`webamp-config.json` beside the exe; device stored by name+api, resolved to index at startup). Input channel (IN 1/IN 2) live; non-ASIO device switches live; **ASIO device changes persist + apply on restart** (ASIO can't reliably re-init mid-process — verified switch-away works, switch-back defers, restart opens saved device fresh at 5.3 ms). UI: sections grouped Practice/Amp + collapsible (persisted); Cabinet moved next to Amp model; "Levels & I/O" section with input-device / input-channel / output-device pickers + meters + amber "restart to apply" note. ASIO-duplex coercion keeps in/out combos legal (4 cases browser-verified). Tests: ws_test audio block.
- [x] **P3h: Rig auto-persist** — DONE 2026-07-22. The full rig now survives a helper *restart*, not just a page reload (previously only device/buffer persisted; amp params lived in engine memory and reset each launch). `Engine::ampJson()` / `Control::applyAmp()` extend `webamp-config.json` with an `amp` block: in/out gain, gate, bass/mid/treble, NAM model + cabinet IR (by display name), all 5 pedals (enabled/placement/order/params), and metronome bpm/beats/accent/vol. Writes are **debounced** (~400 ms after the last change, flushed from the meter loop + on graceful shutdown) so knob drags don't thrash the disk; device/buffer persistence is unchanged and preserved alongside. **Deliberately transient** (never persisted): `mute` (always starts muted — safety), `tunerOn`, metronome on/off. Restored at startup over the freshly-loaded defaults. Verified: WS round-trip — changed gain/tone/gate/model/IR/pedal/metro → confirmed written to config → restarted helper → every value restored, `mute` still true. Rebuilt + reinstalled (`dist/webamp-setup-0.2.0.exe`). Foundation for Presets (a named rig = this blob, keyed).

*Backlog re-prioritized 2026-07-18 per [docs/market-analysis.md](docs/market-analysis.md):*
- [x] **Pedalboard v1** — DONE 2026-07-18. `helper/src/pedals.h`: 5 real-time-safe pedals (compressor, overdrive, chorus, feedback delay, mono Freeverb reverb), fixed roster, each placeable pre/post-amp + reorderable + bypassable, all pre-allocated (no locks/alloc in processBlock). Chain: input→gate→[pre-pedals]→NAM→tone→cab→[post-pedals]→out+metro. Protocol: `setPedal` (enabled/placement/order/params); `pedals` array in state. UI: pedalboard section with Before/After-amp zones, per-pedal LED on/off, move-across (△/▽), reorder (↑↓), 3 knobs each. Tests: `pedals_test` (DSP characteristics — comp attenuates, drive saturates rms/peak 0.71→0.98, delay echoes, reverb tails, chorus modulates) + ws_test setPedal round-trip; **all 5 pedals enabled = 0 xruns over 3 s** (real-time safe under load). *v2 candidates: 2nd drive, BPM-synced delay, parallel paths.*
- [x] **Presets (P3h)** — DONE 2026-07-18. Save/recall named rigs (amp model + cab IR + amp knobs + full pedal chain) to `webamp-presets.json` beside the exe. Protocol: savePreset/loadPreset/deletePreset; names in state. Tone-only (excludes metronome/tuner/mute/device). UI: Presets section atop the Amp group (name box + Save, load/delete dropdown, auto-selects the just-saved rig). Round-trip verified (model + knobs + pedals restored exactly). *Fixed an nlohmann `.items()`-on-a-temporary dangling-ref bug (read back as null) during the build.* Note: live pedal state isn't persisted across restarts by design — presets are the tone-persistence mechanism. Later: shareable as URL/file.
- [ ] **Riff history + auto practice log** — rolling capture buffer ("save last 30 s") + automatic playing-time heatmap; white-space features no amp ships
- [ ] **Model/IR upload from page + TONE3000 browse** (public API; 275K free captures)
- [x] **Drum machine / groove box (P3i)** — DONE 2026-07-18. 16-step sequencer, 5 synthesized voices (kick/snare/crash/hihat/ride) in `helper/src/drums.h`, stepped in the audio callback at 16th notes, synced to the metronome tempo. Own play/stop + volume knob; shares BPM with the metronome. Protocol: setParam drumOn/drumVol, setDrumCell {voice,step,on}, clearDrums; drums state + drumStep in meters for the playhead. UI: grid in Practice group (5 rows × 16 cells, beat grouping, live playhead), Play/Clear. Synthesized (no samples/licensing). **Scope fence held: no velocity/swing/kits/song/import/export.**
- [x] **Drum grid expansion (P3j)** — DONE 2026-07-18. Selectable time signature (3/4 or 4/4), 1–4 bars, resolution quarter/8th/16th/32nd (up to 128 steps). Pattern = 128-bit masks/voice; changing layout clears it. Working pattern persisted to `webamp-drums.json` (survives restart). Protocol: setDrumGrid {beatsPerBar,bars,subdiv}. UI grid is variable-width + scrollable with sticky row labels and bar/beat grouping.
- [x] **Named groove library (P3k)** — DONE 2026-08-06. The drum analogue of presets: save the current grid+pattern under a name, switch between them. Working pattern is the scratch pad; grooves are named snapshots in `webamp-grooves.json` (survives restart). Protocol: saveGroove/loadGroove/deleteGroove; `grooves` names in state. UI: groove section under the drum grid (select-to-load, Update, Save as new, Delete) mirroring the presets UX. Loading a groove restores both layout and pattern. Verified: ws_test groove block + full save→restart→load→delete round-trip; installed build confirmed. Scope fence still held (no velocity/swing/kits/song/export).
- [ ] **Looper v1** — quantized bar-snap, ~60 s, over the drum groove (Spark 2's headline feature; absent from all browser amps)
- [ ] **Phone-as-remote polish** — optional LAN bind + pairing security ("no pairing" vs everyone's Bluetooth pain)
- [ ] *(parking lot, deliberately LAST — after all known features)* **AI integration survey** — candidates identified 2026-07-18: text-to-tone as a *selection* problem over the TONE3000 library (prompt → model + IR + chain preset; cheaper than FUKKAUDIO's synthesis approach), practice-coach feedback over riff history ("your timing drifted on the fast section"), auto-chord detection for play-along (Spark's killer feature; real ML work), smart preset suggestions. Prerequisites: pedalboard, presets, riff history all shipped.
- [x] ~~Real UI design~~ — *superseded by P4b (React + shadcn/ui port).*
- [x] ~~macOS: likely no helper needed~~ — *superseded 2026-08-08: the helper now builds and runs on macOS natively (P4a); the in-browser WASM engine became the cross-platform demo/funnel tier instead (P4c).*
- [ ] Auto-update strategy
- [ ] *(parking lot)* **Rust helper rewrite** — feasible as of 2026-08 with zero C++ FFI: `nam-rs` 0.4 (pure-Rust NAM incl. A2, 1e-5 parity, RT-safety test-enforced — but young, unbenchmarked vs the C++ A2 fast path), `fft-convolver` (Rust port of our exact IR convolver), `cpal` (CoreAudio/WASAPI/ASIO; ASIO least proven vs our measured 7.62 ms on PortAudio). Not worth a rewrite while the C++ helper is done and stable; revisit if maintenance hurts or nam-rs proves faster. C++ core stays where new NAM architectures land first.

## P4 — Cross-platform + production web *(planned 2026-08-08)*

- [x] **P4a: Cross-platform refactor + macOS port** — DONE 2026-08-08 (`f0250cf`). Monolithic `helper.cpp` (1427 lines) split into `src/engine/` (Engine/DSP chain, device+stream handling, JSON control plane — platform-neutral, extracted verbatim), `src/dsp/` (relocated DSP headers), `src/platform/` (six-function OS shell: Windows keeps tray/mutex/log-redirect; mac = console app, Ctrl+C quits), and a slim `helper.cpp` orchestrator with zero `#ifdef`s. CMake gates ASIO SDK + WASAPI/WDM-KS + p1a/p1b behind WIN32; mac gets CoreAudio for free (`paCoreAudio` needs no ifdef — PortAudio defines the full host-API enum everywhere). **Verified on macOS (arm64): DSP tests pass; helper runs end-to-end — web UI connects, metronome plays through CoreAudio, models/IRs load, device picker lists CoreAudio devices.** Binary: 1.5 MB. *Known quirks: xruns tick up with unsynced default in/out devices (mic+speakers — PortAudio full-duplex clock-drift quirk; one real interface as both should be fine); exit code 1 after clean shutdown (cosmetic, see below).*
- [x] **P4b: React + TypeScript + shadcn/ui port** — DONE 2026-08-09. Rebuilt the 1198-line vanilla `web/index.html` as a Vite + React 19 + TS + Tailwind 4 + shadcn (Base UI) SPA; the helper now embeds and serves the committed single-file build. All 8 sub-tasks:
  - [x] *P4b-1 scaffold* — pnpm + Vite 8 + React 19.2 + TS strict + Tailwind 4 + shadcn (Base UI variant, user's preset: IBM Plex Sans/Geist Mono, indigo, dark default); `vite-plugin-singlefile` → one 567 kB dist/index.html so `embed_file.cmake` keeps working (helper still embeds the legacy file until P4b-8 cutover; root `.gitignore` `dist/` rule must be relaxed then to commit the artifact).
  - [x] *P4b-2 engine layer* — `web/src/engine/`: protocol.ts (typed mirror of control.h — the only file touching raw JSON), Engine interface (helper vs future demo hidden from UI), HelperEngine (hello-on-open, 2 s reconnect, 33 ms param coalescing), DemoEngine stub, zustand store + version-mismatch flag. Fast streams (meters/tuner) bypass React state everywhere — imperative refs only.
  - [x] *P4b-3 app shell* — header (status chip, engine info, live mini-meters, theme toggle incl. `d` key), amber input-gate banner (safety mute UX ported), offline card, Play/Practice/Settings tabs with localStorage persistence.
  - [x] *P4b-4 custom controls* — Knob (faithful makeKnob port: 270°, drag range/150 px, wheel, dbl-click reset, drag-local value beats server echo), BeatDots, TunerNeedle, DrumGrid (optimistic toggles, imperative playhead). Dev playground at `?dev` binds all to the live engine; verified round-trips against the running helper. Post-review (react.dev "You Might Not Need an Effect"): React 19.2 stable `useEffectEvent` + shared `useMeters`/`useTuner` hooks replaced all ref-mirror effects.
  - [x] *P4b-5 Play view* — presets, amp/cab wrap-around steppers, 6 tone knobs, pedalboard (LED enable, chain reorder, cross-amp move, param knobs). Shared `lib/rig-specs.ts`; engine gained a keyed coalescing queue shared by setParam + setPedalParam.
  - [x] *P4b-6 Practice view* — tuner, metronome (typed/±step tempo, tap, Space/↑/↓/T keys, accent, click knob), drum machine (transport, grid selects, grooves). Extracted generic `NamedLibrary` (shared by presets + grooves) and `useTapTempo`.
  - [x] *P4b-7 Settings view* — device/channel/buffer pickers (Base UI Select needs `items` map for labels), ASIO-duplex coercion in `lib/audio-devices.ts` (no-op on CoreAudio), buffer pending-restart flow, engine info + honest latency caveat.
  - [x] *P4b-8 parity pass + cutover* — DONE 2026-08-09. ws_test.py ALL PASS (protocol untouched). CMake embed repointed legacy → `web/dist/index.html` (committed build; root `.gitignore` `dist/`→`/dist/` so the artifact tracks). Helper rebuilt + serves the React UI at 127.0.0.1:43718, verified connected end-to-end with no external requests (fonts inlined as data URIs). Legacy `web/legacy/index.html` removed (in git history). Also: aligned `typecheck` script to `tsc -b` (was missing the app tsconfig; caught an erasable-syntax + Base UI null-value error the prod build then surfaced).
  Dev notes: `pnpm --dir web dev` + `./helper/build/webamp-helper --assets assets`; new UI connects to the live helper at localhost:5173. After any UI change run `pnpm --dir web build` to refresh the embedded `web/dist/index.html`, then rebuild the helper. Embedded browser panes report `visibilityState:"hidden"` → Chrome throttles timers to ~1 s — don't trust latency measurements from there.
- [ ] **P4c: WASM demo engine** *(blocked by P4b; de-risked 2026-08-08)* — **TONE3000 ships an official MIT npm package `neural-amp-modeler-wasm`** (github.com/tone-3000/neural-amp-modeler-wasm): NAM inference in WASM with AudioWorklet integration done, plus a React reference component — replaces our planned hand-rolled Emscripten build. Our work: wrap it behind the Engine interface, `getUserMedia` input (echoCancellation/noiseSuppression/autoGainControl **off**, device picker), cab IR via native `ConvolverNode`. Scope: amp path only. A2 perf headroom (A2-Lite = 50% of a $3 Cortex-M7) means real-quality models in-browser — latency stays the only differentiator vs the helper, which is the honest funnel story. Expected RTL: ~20–40 ms mac / 40–80 ms Windows. Optional later: Emscripten build of our `src/dsp/` for gate/tone/pedals in the demo tier.
- [x] **P4d: Owned starter tone pack** — DONE 2026-08-23. Instead of self-capturing, found a redistributable pack (research: no CC0 *capture* ecosystem, but the NAM project ships one). `helper/starter/`: **`Obsidian (Omega Ampworks).nam`** — hi-gain capture by Steven Atkinson, MIT-licensed (it's the sample model bundled in NeuralAmpModelerPlugin's `REAPER/model.nam`; MIT-NAM.txt carries the notice); attribution kept per user. **3 cab IRs** (`4x12 Modern/Full/Tight.wav`) from **Jester's Brutal Pack 1.0 — CC0/public domain** (darwinscat.com), renamed from the originals (several were NSFW) with source names recorded in `helper/starter/LICENSES.md`. Verified: helper loads "1 models, 3 IRs → Obsidian + 4x12 Full" at 48 kHz; license `.md`/`.txt` correctly ignored by the asset scanner. Same files will serve the hosted demo (P4e) by URL. *One good hi-gain tone is fine for v1; add a clean tone later if wanted.*
- [~] **P4e: Landing page + hosted /app + onboarding funnel** — **Landing page DONE + LIVE 2026-08-23 at https://riffamp.app** (HTTP/2 200, Cloudflare, valid auto-renewing cert, edge-cached ~66ms, `_headers` applied). Host: **Cloudflare Workers static-assets** (`wrangler.jsonc` at repo root: name `riffamp`, `assets.directory ./site`). NOTE: chose **Workers, not Pages** — as of 2026 Cloudflare steers new static sites to Workers (Pages still works but is no longer where investment goes); ignore earlier Pages phrasing. Deployed by direct `wrangler deploy` (needs the user's `wrangler login` — agent can't auth). Domain registered at Cloudflare Registrar; custom domain attached; USPTO-clear. `site/` = hand-written static SEO landing (`index.html`, `_headers` w/ COOP/COEP stub for `/app`, `robots.txt`); verified desktop+mobile. **REMAINING:** (a) **push-to-deploy not yet wired** — currently manual `wrangler deploy`; optionally connect the private repo in dashboard → Workers & Pages → Builds (Worker name must = `riffamp`) for deploy-on-push. (b) **Download buttons — DONE 2026-08-30 (Windows side, commit 2109a41):** repo made public, Windows installer shipped as **GitHub Release v0.2.0** (`riffamp-setup-0.2.0.exe`, 3.26 MB); landing page's Windows button links straight to the release asset + carries the unsigned-app "More info → Run anyway" note; `docs/tester-guide.md` added. Live & verified. macOS button still "coming soon" (no Mac installer yet). (c) **`/app`** = the big next chunk — IN PROGRESS from 2026-08-30, see the plan block below. (d) optional tidy: enable *Always Use HTTPS* (moot — `.app` is HSTS-preloaded) and a `www→apex` redirect.
  - **P4e/app plan (2026-08-30):** Library **confirmed**: `neural-amp-modeler-wasm` (TONE3000 fork of NAM core, **MIT**) — provides NAM amp inference **and cab IR** in a single AudioWorklet, exposes a **low-level engine API** (`neural-amp-modeler-wasm/engine`, separate from their `T3kPlayer` React component — we use the low-level one so our existing `Engine`-interface UI works unchanged), loads `.nam` by URL, and **needs NO COOP/COEP / SharedArrayBuffer** (v2 avoids it for WebKit). So the `_headers` cross-origin-isolation stub stays OFF, and the `helper/starter/` pack (Obsidian `.nam` + 4x12 IRs) serves the demo by URL. **Demo scope (user-chosen): "amp wow + practice tools"** = NAM amp + cab IR + tone stack (WebAudio Biquad EQ) + metronome + tuner + picking trainer (the wedge, latency-tolerant); **deferred**: pedals, noise gate, drums (no free WebAudio equivalent). Phases: **(1) build fork** — helper build stays single-file (`viteSingleFile`, compile-time embed); hosted build drops single-file, `base:"/app/"`, normal chunks + wasm/worklet assets, output → `site/app/` (`vite build --mode hosted`). **(2) DemoEngine** — replace the offline stub: `getUserMedia` guitar in → NAM worklet → out; implement `setParam`/meters against the existing `Engine` interface; tone stack via BiquadFilterNodes; metronome/tuner/picking in-browser (picking = port the flux onset idea to JS/worklet). **(3) engine selection** in `EngineProvider` — hosted origin probes `ws://127.0.0.1:43717` briefly → helper if reachable (+pairing UI), else DemoEngine; local origin → helper as today. **(4) pairing UI** (P4f client) — 6-digit entry on `needPair`, retry on `pairFailed`. **(5) funnel UX** — "browser demo (~30–50ms) → install for <10ms" upsell + mic-permission gate. **(6) deploy+verify** — build → `site/app/`, `wrangler deploy`, verify mic/tone live.
  - **P4e/app PROGRESS (2026-08-30):** Phases **1–3 + practice tools DONE** (typecheck+lint clean, both builds verified). **(1) Build fork** ✓ — `vite.config.ts` mode-forks: `pnpm build` (mode production) = helper single-file `web/dist/index.html`, demo **excluded** (verified 0 wasm/NamEngine refs — `__DEMO__` const-fold + dynamic import + `copyPublicDir:false`); `pnpm build:app` (mode hosted) = multi-file → `site/app/` (base `/app/`, wasm+worklet as assets, demo in a lazy 16.7KB chunk). `site/app/` gitignored (build output); `web/public/nam/` holds the demo's owned assets (obsidian.nam + 3 IRs, clean URL names). **(2) DemoEngine** ✓ — `web/src/engine/demo/`: `audio-graph.ts` (mic→inGain→NAM→ConvolverNode(IR)→3×Biquad tone→outGain→analysers→out; guitar-safe getUserMedia; NAM amp-only so cab IR = native ConvolverNode), `demo-engine.ts` (Engine impl), `default-state.ts`, `metronome.ts` (lookahead scheduler + beat tracking), `tuner.ts` (octave-safe autocorrelation), `picking.ts` (spectral-flux onset detect + live PickingMessage + pick-run recorder). Scope: amp+IR+tone+metro+tuner+picking; pedals/gate/drums are no-ops. **(3) selection** ✓ — `engine-provider.tsx` folds to `HelperProvider` (helper build) or `DemoAwareProvider` (dev/hosted: probe `ws://127.0.0.1:43717` → helper else demo; `?engine=demo|helper` override). **VERIFIED headlessly (device-less browser pane, OfflineAudioContext):** NAM amp loads + transforms a 220Hz sine (in RMS .141→out .251, changed) at 48kHz; all 4 assets serve 200; tuner detects E2/A2/D3/G3/B3/E4/A4 correct within ~2¢ (fixed an octave/subharmonic bug). **NOT yet verified (needs real guitar on user's machine — pane has no mic/audio device):** live mic→amp→speakers tone, and the **flux onset detector accuracy** (16ths-vs-triplets) — same real-ear verification the native trainer needed. **(5) funnel UX DONE (2026-08-30):** `demo-start-gate.tsx` (branded click-to-start — satisfies the AudioContext/mic user-gesture requirement; demo waits behind it), `demo-funnel-banner.tsx` (slim "you're on the free browser demo → install the helper for <10ms" bar atop the app when kind==demo), `offline-card.tsx` now demo-aware (mic-blocked message + Try-again that re-runs start() + install link; helper message unchanged). Demo starts **muted** (like the helper). Verified in-pane: gate renders, click → mic prompt → (denied) → demo-aware error card + banner all correct. Also: `select-engine.ts` `isLocalOrigin()` — the helper probe now runs **only on local origins** (dev/localhost, where P4f trusts loopback with no pairing); a remote https origin (hosted /app) goes straight to the demo, avoiding the broken needPair-hang until the pairing client ships. **REMAINING for P4e:** (4) **pairing UI client** (the only unbuilt phase) — code-entry on `needPair`, retry on `pairFailed`, and then flip remote origins to try the helper too; verifiable on this Mac by building+running the helper. (6) **deploy** — `pnpm --dir web build:app` (done, output in `site/app/`) then `wrangler deploy` (needs user's `wrangler login`). **USER GUITAR VERIFICATION PENDING** (live tone + flux onset accuracy). Nothing committed yet.
- [x] **P4f: Hosted-origin security — pairing codes (helper side)** — DONE 2026-08-09. New `helper/src/engine/pairing.h`: local origins (empty/localhost/127.0.0.1) trusted automatically; any other origin (the hosted app) opens but is **untrusted** and gets `{type:"needPair"}` — it can only send `{type:"pair",code}`; the helper prints a random 6-digit code at startup (`isLocalOrigin` moved here, replacing `originAllowed`). Correct code → origin approved + persisted to `webamp-paired.json` (one-time; reconnects auto-trusted); wrong code → `{type:"pairFailed",attemptsLeft}`, socket closed after 5 tries. Per-connection trust tracked by socket ptr in helper.cpp; **all broadcasts (state/meters/tuner) go only to trusted clients** so an unpaired origin never sees device names/rig/levels. Protocol types added to `web/src/engine/protocol.ts`. Tests: new `helper/tests/pairing_test.py` (local-trusted / needPair / wrong→fail / right→state / persist-across-reconnect) ALL PASS; `ws_test.py` updated (non-local origin now gets needPair, not outright close) — ALL PASS. Also fixed mac stdout to line-buffered so the code shows promptly in redirected logs. **Deferred to P4e** (client side): the code-entry UI + auto-switch, and Chrome's LNA permission-chip UX (P2b finding e). *Windows: code currently only in the log; tray display is later polish.*
- [x] **P4g: Asset licensing hygiene** — DONE 2026-08-23. (1) Installer bundles the owned `helper/starter/` pack, not `assets/*` (P4d + `webamp.iss`) → distributed installers carry no copyrighted files; no trademarked display names in the starter pack. (2) **Git history purged**: `git filter-repo --path assets --invert-paths` dropped the copyrighted top-level `assets/` (commercial greenback IRs + TONE3000 captures) from **all** history — verified 0 asset objects across all refs — and force-pushed (all commit SHAs changed; master 80c5d68→c360c82). The files stay on disk for local dev and are now **gitignored** (`assets/`) so they can't be re-committed. So the repo is now safe to open-source/go-public with no licensing landmine, regardless of the eventual decision. README run example repointed to `helper/starter`. **Coordination:** the Windows clone has divergent history — back up its local `assets/`, then `git fetch && git reset --hard origin/master` (or re-clone). Recovery net: pre-rewrite `.git` at `/tmp/webamp-git-backup`, assets at `/tmp/webamp-assets-backup` (mac). **Remaining (→ folded into P4j):** drag-and-drop `.nam`/`.wav` import UI + "get more tones"/TONE3000 browse. Rebuild the installer on the PC (`ISCC.exe`) so `dist/webamp-setup-*.exe` picks up the clean pack + delete the stale one.
- [ ] **P4j: TONE3000 API integration — in-app tone library** — official public REST API (OAuth 2.0, publishable key, MIT SDK + demo app at github.com/tone-3000/api; same path HeadRush/Blackstar use): search/filter the library, download `.nam` on behalf of the logged-in user. User downloads under *their* TONE3000 account/terms → webamp is never the distributor; "recommended starter tones" = curated tone IDs with author attribution. Rate limit 100 req/min. Caveat: downloads need user login → P4d's single bundled tone covers first-run (no account, offline). Supersedes the "Model/IR upload from page + TONE3000 browse" backlog item's browse half. Works from the WASM demo app too.
- [ ] **P4h: Re-verify Windows build after the refactor** — first Windows build since the P4a split (extraction was verbatim but Windows-unverified). On the PC: pull master, `cmake -S helper -B helper/build` + `cmake --build helper/build --config Release` (VS2022; full paths per P1 machine notes: `C:\Program Files\CMake\bin\cmake.exe`). **No Node needed** — the embedded UI is the committed `web/dist/index.html`. Verify: builds clean; tray icon + menu; ASIO device opens (saved webamp-config.json should restore Komplete); **UI at 127.0.0.1:43718 is now the React app** (tabs Play/Practice/Settings + mixer footer — replaces the old single page); `ws_test.py` + `pairing_test.py` + `picking_test.exe` all pass (tests need `pip install websocket-client`; pairing_test needs the code from the console/log — Windows logs to `webamp-helper.log` beside the exe; delete `webamp-paired.json` after). Watch for: platform_win.cpp tray behaviors (moved verbatim), `/SUBSYSTEM:WINDOWS` linking, WASAPI/WDM-KS enumeration in Settings. *P4k A2 re-check and P5a calibration ride along with this session.* **RENAME (P4l) coordination — do first on the PC:** the GitHub repo is now `raduliviu/riffamp` (redirects work, but run `git remote set-url origin git@github.com:raduliviu/riffamp.git`); after pull the binary is **`riffamp-helper.exe`**, config/log files are `riffamp-*` (old `webamp-*` orphaned — fine), tray/mutex strings say RiffAmp, and the installer is **`installer/riffamp.iss`** → `dist/riffamp-setup-*.exe`. So above, read `riffamp-helper.log` / `riffamp-paired.json` / `riffamp-config.json` for the old webamp-* names. (The platform_win.cpp string edits were done on the Mac but only compile on Windows — this build is their first check.)
- [x] **P4k: A2 smoke test** — DONE 2026-08-08 (macOS). NAM Architecture 2 works out of the box: A2 inference landed in NeuralAmpModelerCore v0.5.2, we pin v0.5.4 with `NAM_ENABLE_A2_FAST` defined. Verified with `example_models/wavenet_a2_max.nam` (the everything-enabled A2 test model, config version 0.6.0): loaded, ran real-time @48 kHz/128 on CoreAudio, 1 xrun, UI round-trip fine. → TONE3000 A2 captures (P4j) work without engine changes. A2-Full = 30–40% less CPU than A1; quality beats NDSP V2 / ToneX V2 in TONE3000's blind tests. *Windows re-check rides along with P4h.*
- [x] **P4i: Exit code 1 on clean shutdown — NOT A BUG** — investigated 2026-08-09. The helper shuts down cleanly: SIGINT *and* SIGTERM both print the full sequence and exit **0** (verified `kill -INT`/`-TERM` on the helper's own PID as a plain `&` job; even `pkill -INT -f webamp-helper` → exit 0). The recurring "exit code 1" was an artifact of the agent's `run_in_background` task wrapper reporting a killed wrapped job — not the helper's own return. No code change.
- [x] **P4l: Internal rename webamp → riffamp (user-visible)** — DONE 2026-08-23 (Mac; no Windows work in flight, config orphaning OK per user, so done now not post-hosting). Renamed everything users see: binary `webamp-helper`→**`riffamp-helper`** (CMake target/project); config/log/capture filenames `webamp-*`→`riffamp-*` (helper.cpp; **old files orphaned, no migration** — single user); Windows tray tooltip/menu/MessageBox/mutex/log + install strings (platform_win.cpp — pure string literals, compile-verified on PC at P4h); installer `webamp.iss`→`riffamp.iss` (AppName/dir/output/exe/icon/shortcuts/autostart); resources `webamp.rc/.ico`→`riffamp.rc/.ico`; the React app's `<title>` + `WEBAMP`→`RIFFAMP` wordmark + all `localStorage` keys `webamp:`→`riffamp:` (orphans UI prefs); README product name + run/build commands. **Verified (Mac):** riffamp-helper builds + runs, serves `<title>RiffAmp>`/RIFFAMP wordmark, binary carries riffamp-*.json literals (no webamp-), picking_test ALL PASS. **Kept as-is (invisible, out of scope):** C++ `namespace webamp`, `WEBAMP_DSP_DIR` CMake var, `web/package.json` name `webamp-web`, repo/dir name (GitHub rename = cosmetic, later). Windows side: normal `git pull` (not a history rewrite); rebuild picks up riffamp-helper.exe + rerun ISCC on riffamp.iss.

## P5 — Practice instrument

- [~] **P5a: Picking trainer** — built 2026-08-09; **real-guitar calibration pending (user)**. Use case: at 180+ BPM the ear can't tell 16ths from triplets — measure it. `dsp/picking.h` OnsetDetector (rise detection: instant-attack peak follower vs ~10 ms lagging copy — decay can't retrigger by construction; 25 ms refractory; sens 0..1 → ratio 3.0..1.0) runs on the clean pre-amp input in the audio callback; onset + metronome-click timestamps go into lock-free rings against a sample clock (the metronome IS the reference grid — same audio stream, no sync problem). `engine/picking_tracker.h` (control thread, ~12 Hz alternating with tuner) → `{type:"picking"}`: notes-per-beat from median IOI over the last bar, evenness (IOI cv), onset/click ages for the UI. Params pickOn/pickSens (transient). UI: Practice → PICKING TRAINER — target subdivision select (8ths/triplets/16ths/sextuplets, persisted), big color-coded npb readout (green on target / amber drifting / red off), evenness %, canvas timeline strip (clicks = grid lines, picks = ticks, 8 beats). Tests: `picking_test` offline (16 synthesized cases: 16ths/triplets@180 exact counts + npb 4.00/3.00, cv discrimination, accents, 240 BPM, no double/false triggers) ALL PASS; ws_test picking block ALL PASS; **acoustic end-to-end verified**: metronome clicks → speakers → MacBook mic → detector → UI read exactly 1.0 notes/beat. **Calibration session (Windows PC, where the interface lives — rides with P4h):** plug in, Practice → PICKING TRAINER → Enable, target 16ths, metronome on at a comfortable tempo, alternate-pick one string; then push to 180+. Expect npb ≈ 4.0 green; deliberately play triplets → should read ~3.0 red. **Symptom → lever map** (all in `helper/src/dsp/picking.h`, rebuild `webamp-helper` after edits — no Node needed, UI is prebuilt):
  - *Missed notes (npb reads low, gaps in timeline)* → raise SENS knob first (pickSens → ratio 3.0..1.0); if maxed and still missing, lower `kMinLevel` (0.003 ≈ -50 dBFS; weak passive pickups may need 0.001) or shorten `preK` lag (0.010 s).
  - *Double-triggers (npb reads high, paired ticks ~25 ms apart)* → raise `refractorySamples` (0.025 s → 0.035; ceiling: at 240 BPM 16ths IOI is 62 ms).
  - *Ghost onsets while not playing* (amp hum, pick scrape) → lower SENS, or raise `kMinLevel`.
  - *Retriggers on vibrato/sustain* → shouldn't happen by design (rise detection); if seen, lengthen `preK` slightly (0.010 → 0.015) — and tell the log what signal caused it.
  - *Palm mutes detected but ringing/open notes missed* → FIXED 2026-08-13 (user field report): the detector now listens through a **~1.8 kHz one-pole high-pass** (`hpA` in picking.h) — the ring is tonal (fundamentals ≤1.3 kHz) and vanished above the filter, while the broadband pick transient passes, so open notes rise from a low floor like palm mutes. Repro test: "ringing 16ths @160" (was 3/16, now 16/16). If ringing notes are still missed on real guitar, lower fc (1800 → 1200); if pick-scrape ghosts appear, raise it.
  - *Way too many notes (scrape/buzz ghosts after the HPF)* → FIXED 2026-08-13 (2nd field report; the HF band alone is where pick scrape, fret buzz, and string noise live). Detector is now **dual-band with two vetoes** (the Rocksmith lesson: use your priors — they had the chart, we have the expected rate + note energy): (a) **full-band support gate** — a trigger also needs `fbFast > fbPre × kFullSupport` (1.15, ~1.2 dB): a real pluck adds energy to the whole signal, a scrape on a ringing note barely moves it; (b) **expected-rate refractory** — `setMinGap(0.45 × 60/(bpm × pickTarget))` clamped 25–90 ms; the UI's target subdivision now flows to the engine as `pickTarget` (setParam, 1–8), so at 16ths@160 nothing can trigger within 42 ms of a real note. `kMinLevel` is now the HF-band floor (0.004). Tests: "messy 16ths" (scrape 12 ms before + fret-buzz bursts after each note) exactly 16/16; "scrape on a ring" adds nothing; all prior cases green. Levers if still off on real guitar: kFullSupport 1.15 (raise → fewer ghosts, may miss fast same-string runs), the 0.45 gap fraction, fc, SENS.
  - *Ringing notes STILL missed after the dual-band fix* → FIXED 2026-08-13 (3rd field report): the full-band support gate is itself a rise detector, so a loud ring bed (several strings sustaining) held the full-band envelope up and vetoed real notes — the palm-mute bias reborn. Now the veto **only applies to weak HF transients**: `attackRef` (decaying max of hfFast, τ 2.5 s) tracks the recent loudest attack; anything above `kStrongFrac` (0.33) of it is unmistakably a pick attack (scrape/buzz sit at 15–25%) and bypasses the full-band gate regardless of bed level. Repro test "chord bed" (3 strings ringing under a 16th run): 13/16 → 16/16; all ghost tests still green. Levers: kStrongFrac (lower → more bypass, more ghosts), attackRef τ. Caveat: deliberately soft notes (<33% of recent loudest in HF) over a loud bed may still be missed — raise SENS or lower kStrongFrac if that bites.
  Note: detector taps the input **post input-gain** (`raw = in * gainIn`), so the GAIN knob scales the signal vs fixed `kMinLevel` — set normal playing gain before judging sensitivity. `picking_test` must stay ALL PASS after any constant change (adjust tests only for deliberate spec changes). **File map** — engine: `helper/src/dsp/picking.h` (detector + IOI stats), `helper/src/engine/picking_tracker.h` (drain rings → message), `engine.h` (integration: pickOn/pickSens atomics, sampleClock, onsetTs/clickTs rings, hooks in process()), `control.h` (params), `helper.cpp` (meter-loop broadcast, alternates with tuner); tests: `helper/src/picking_test.cpp`, ws_test.py §6b; web: `web/src/views/practice/picking-section.tsx`, `web/src/components/controls/picking-timeline.tsx`, protocol/engine/use-streams additions in `web/src/engine/`. UI changes need Node ≥22 + pnpm: `pnpm --dir web build`, then rebuild the helper (embeds `web/dist/index.html`). Later ideas — per-note timing deviation score vs the click grid, free-time mode (no metronome), sens persistence, practice-log integration.
- [x] **P5b: Pick run — count-in, record N bars, per-bar breakdown** — DONE 2026-08-13 (Windows). Use case: "I play an 8-bar riff and can't tell *where* it falls apart (transitions, fatigue)." One click (`● Record run`) starts everything: onset detector on, **metronome force-restarted at beat 1** (`Engine::metroRestart` one-shot → `metro.wasOn = false`), count-in (1–2 bars) then recording (1–16 bars), **auto-stop at the boundary click**, result broadcast. Engine: `engine/pick_run.h` `PickRun` — control-thread state machine (mutex-guarded; WS handlers start/cancel, meter loop feeds via independent ring cursors + `drainRing`); grid = **actual click timestamps** (immune to bpm rounding); result finalizes half a beat after the boundary so late-drained onsets land; onsets kept with a half-beat margin either side (anticipated first note = negative ms). Protocol: `startPickRun {bars 1-16, countIn 1-2}` / `cancelPickRun`; `pickRun` progress ~12 Hz (phase/bar/beat) + one `pickRunResult` (raw clicks+onsets, ms); `pickRunActive` in state; **stopping the metronome mid-run cancels the run** (it's the run's clock). All scoring client-side in `web/src/lib/pick-analysis.ts`: subdivision grid interpolated between clicks, onsets attributed to nearest gridpoint → per bar count vs expected, evenness (IOI cv), signed mean offset (rushing/dragging); verdicts good/ok/bad. UI (`picking-section.tsx` + `pick-run-results.tsx`): BARS + COUNT-IN selects (persisted), live status (COUNT-IN · n / ● BAR x/y), verdict-colored bar strip ("0/8 bars clean · roughest: bar 5"), click a bar → detail (counts, evenness, tendency, onset-vs-grid mini strip). Free-play Enable now also starts the metronome (was 2 clicks). Tests: `picking_test` +20 PickRun cases (boundary end, count-in gating, stale-click fence, margins, cancel) ALL PASS; ws_test §6c end-to-end live run (progress → result → metronome auto-off, both cancel paths) ALL PASS; UI verified in-browser (run → status → red 0/16 bar cell → detail). Later: persist run history (→ practice log), wrong-note detection needs pitch tracking (separate research).

- [x] **P5c: Detector calibration + spectral-flux rework** — DONE 2026-08-16 (Windows, four field rounds with the user's guitar). Level-based onset detection declared **information-starved** on real guitars: labeled captures proved soft up-picks measure the same amplitude as pick scrape (~20% of a down-pick), and real polyphonic ring *beats*, defeating every envelope-rise gate in one direction or the other. **Replaced with spectral flux** (`dsp/flux.h`: self-contained 512-pt radix-2 FFT, hop 256, log magnitudes, SuperFlux positive diff vs 3-bin max-filtered prev frame, median+MAD adaptive threshold — mean+std self-poisons on regular playing — plus `magFloor` spectral noise gate — log compression otherwise turns noise-floor wiggle into flux; a silent count-in fired steadily — `binFloor` per-bin gate, relative + absolute floors, expected-rate min gap). **Calibrated by joint parameter sweep on a labeled capture** (user: silent count-in, then exactly 32 16ths @105): winner minGap 0.8×subdiv / binFloor 0.6 / magFloor 0.4 → **30/32 notes, 3 ghosts, 14.5 ms mean error** (the level detector scored 17/32 on the same take; remaining ghosts ≈ finger-damp contacts). **Integrated on the control thread**: the meter loop drains the raw-input ring (tunerPos advances in lockstep with sampleClock → ring positions map to absolute sample times), runs flux, and writes onsets into the onset ring the tracker + run feeder drain the same tick; gate fraction is run-aware (0.8 during a pick run, 0.45 free play, from tempo × pickTarget); the audio callback no longer detects anything (OnsetDetector deleted from picking.h — stats helpers remain). Tooling that got us here: capture rides the pick run (labeled ground truth), `picking_test <wav> [sens minGap bpm target binFloor peakFrac magFloor]` runs the real detector offline with grid-fit scoring; synthetic exact-count tests rebased to ±2 tolerance (additive plucks proved unrepresentative 4×; the local `captures-*.wav` files are the calibration authority, gitignored). ws_test rpc() now skips all broadcast types (pickRun/captureDone interleave with replies). **Verification run on real guitar: 32/32 notes hit, 4 extras (one = an audible count-in touch), 20.9 ms mean error — user confirms it feels accurate on ringing takes too.**
- [ ] **P5d: Trainer fine-tuning (user-confirmed follow-up)** — detection is trustworthy now; remaining polish is mostly in the SCORER, not the detector: (a) per-bar counts can be inflated by off-grid extras (damp contacts) — attribute onsets to their own subdivision slot only, report extras separately instead of inflating "17/16 notes"; (b) revisit verdict thresholds (cv ≤7%, offset ≤20% subdiv, exact count) against real graded runs — they were set before the detector worked; (c) more labeled captures at other tempos/subdivisions (every Record run auto-captures to `riffamp-capture.wav` — copy interesting ones to `captures-*.wav`, gitignored, and grade with `picking_test <wav> [sens minGap bpm target binFloor peakFrac magFloor]`); (d) maybe expose strictness (relaxed/normal/strict) in the UI.
- [ ] **P5e: Practice-log / journal (research parked 2026-08-16, from riffshed.app)** — riffshed.app is a *practice journal* for musicians (no audio; card-based, deliberate-practice). Ideas worth adapting, ranked by fit: **reps not hours** (count repetitions, and — our edge — quality per rep); **calendar heatmap + streaks**, "quiet achievements, not gamified" (matches our aesthetic; this is the existing "auto practice log" backlog item + [docs/market-analysis.md](docs/market-analysis.md) — riffshed validates it); **20/5 focus cycle** (Pomodoro tuned for instruments); **skill-progression model Explore→Integrate→Master** (slow-clean → tempo — maps onto the picking trainer's tempo push); **offline-first, JSON export/import, educator curriculum sharing (.zip)** — our presets/grooves are already local JSON, so "share a practice pack with students" is a real audience. **The wedge (why ours beats a pure journal):** RiffAmp *hears you* — the picking trainer (P5) already measures notes/beat, evenness, timing, so the log can auto-populate with graded results ("20 reps, evenness +12%, stopped slipping to triplets above 170 BPM") instead of self-reported time. A practice journal that grades itself from real playing — no journal app or amp sim does this, and P5 already built the hard part. Builds on P5b pick-run history ("Later: persist run history → practice log"). **Naming note:** riffshed sits in the practice-app space → reinforced moving off "-shed" names (we chose RiffAmp).

## Resolved de-risking (history)

- [x] Browser latency spike — 79.6 ms Chrome / 110.3 ms Firefox ([`../amp-latency-spike/HANDOFF.md`](../amp-latency-spike/HANDOFF.md))
- [x] Deep feasibility research, pure-browser = dead end on Windows ([`../amp-latency-research/REPORT.md`](../amp-latency-research/REPORT.md))
- [x] Hybrid architecture research (LNA rules, ASIO licensing, DSP licenses, prior art)
- [x] P0: native-path latency proof — WASAPI-exclusive 14.4 ms @64 ([`../amp-helper-p0/`](../amp-helper-p0/))
